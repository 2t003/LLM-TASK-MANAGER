# =============================================================================
# Variables d'environnement — LLM Task Manager
#
# Copier ce fichier en .env et adapter les valeurs.
# En production (Cloud Run), ces variables sont injectées via --set-env-vars
# et --update-secrets dans la commande gcloud run deploy.
# =============================================================================

# ---- Projet GCP (pour le script deploy.sh) ----
GCP_PROJECT_ID=mon-projet-gcp
GCP_REGION=europe-west1

# ---- Cloud SQL ----
# Option 1 : variables individuelles (recommandé pour Cloud Run)
DB_USER=postgres
DB_PASSWORD=changeme-use-secret-manager
DB_NAME=llm_task_manager
INSTANCE_CONNECTION_NAME=mon-projet-gcp:europe-west1:llm-task-db

# Option 2 : URL complète (dev local avec Postgres, ou CI)
# DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5432/llm_task_manager

# ---- Sécurité API ----
# Clé API pour l'authentification REST (header X-API-Key)
API_KEY=changeme-generate-a-strong-key

# ---- Cloud Run (script deploy.sh) ----
SERVICE_NAME=llm-task-manager
ARTIFACT_REPO=llm-task-manager
IMAGE_NAME=llm-task-manager

# ---- Secrets (noms dans Secret Manager) ----
DB_PASSWORD_SECRET=llm-task-manager-db-password
API_KEY_SECRET=llm-task-manager-api-key
